#begin INST
   lvl = 100.
   cboucle = CHAR(SENSOR:'NAME':0)
   accname = WORD('RIS00'//cboucle//'BA')
   acc = BASE.'SYSTEMS'.'ACCUMULA'(accname)
   IFTHEN(NUMBER(acc, 'ACML') > 0)
      ml = acc.'ACML'
      mlini = acc.'GEOM'.'MLINI'
      lvl = ml/mlini*100.
   ENDIF
   lvl
#end
