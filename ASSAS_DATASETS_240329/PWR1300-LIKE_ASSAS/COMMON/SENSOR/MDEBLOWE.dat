#begin INST
   m = 0.
   mdeb = 0
   lowe = BASE.'VESSEL'.'MACR'('LOWERPLE')
   DO i (NUMBER 'COMP' lowe) 1 -1
      icomp = lowe.'COMP'(i)
      comp = BASE.'VESSEL'.'COMP'(icomp)
      name = comp.'NAMES'
      IFTHEN(name == 'DEBRIS')
         m = m + comp.'M'
         mdeb = mdeb + 1
         IF(mdeb == 2) EXIT
      ENDIF
   ENDDO
   m
#end